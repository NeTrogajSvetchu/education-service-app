apiVersion: v1
kind: Service
metadata:
  name: education-service-nodeport
  namespace: {{ .Values.namespace }}
spec:
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: {{ .Values.nodePort }}
  selector:
    app: education-service
  type: NodePort

